# Dependencies
node_modules/

# Build output
dist/
build/

# W6 dictionary: generated clips (regenerate with `build.py synth`; canonical
# copies live in gs…-dictionary/clips/) and extraction scratch
dictionary/clips/
dictionary/.work/

# Licensed SMPL-X model + large derived assets — NEVER commit (non-commercial
# license, §11; large binaries). Live on disk only; served from gs…-smplx-model/.
models/
frontend/public/smplx/
*.npz
*.pkl

# Python
__pycache__/
*.py[cod]
.pytest_cache/
*.egg-info/
.venv/
venv/

# Env / secrets (never commit keys — use Secret Manager per §7)
.env
.env.*
*.local
# …but the committed template carries no secrets (only a public GCS URL):
!frontend/.env.example

# Editor / OS
.DS_Store
.vscode/
.idea/
*.log
